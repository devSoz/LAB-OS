#include <bits/stdc++.h>
using namespace std;

void optimalPageReplacement(int pages[], int nPages, int nFrames)
{
    vector<int> frames;
    int nHits = 0;

    for(int i=0; i<nPages; i++)
    {
        cout<<pages[i]<<" ";
        
    }
}

int main()
{
    int pages[] = {3, 2, 1, 2, 3, 6, 4, 7, 2, 3, 4, 2, 1, 5, 7, 6, 2, 6, 2, 2, 3, 3};
    int nPages, nFrames;
    optimalPageReplacement(pages, nPages, nFrames);
}